{
  "name": "@oceanid/cluster",
  "version": "1.0.0",
  "description": "Oceanid Kubernetes Cluster - Production-Ready 2025 Infrastructure",
  "main": "bin/index.js",
  "types": "bin/index.d.ts",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf bin",
    "deploy": "pulumi up --yes --suppress-outputs",
    "preview": "pulumi preview --diff --suppress-outputs",
    "destroy": "pulumi destroy --yes",
    "refresh": "pulumi refresh --yes",
    "stack:init": "pulumi stack init prod",
    "stack:select": "pulumi stack select prod",
    "stack:output": "pulumi stack output --json",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@pulumi/cloudflare": "^6.11.0",
    "@pulumi/command": "^1.1.3",
    "@pulumi/crunchybridge": "file:sdks/crunchybridge",
    "@pulumi/kubernetes": "^4.24.0",
    "@pulumi/pulumi": "^3.206.0",
    "@pulumi/random": "^4.18.4",
    "@pulumi/tls": "^5.2.2"
  },
  "peerDependencies": {
    "@pulumi/pulumi": "^3.0.0"
  }
}
